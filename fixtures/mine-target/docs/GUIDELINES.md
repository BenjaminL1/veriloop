# mine-target guidelines (INPUT only — prose with NO code backing)

Always write clean, readable code.

That statement is rule-shaped (it says "always"), but it compiles to NO checkable
query — there is no site it could ever mark as broken. A rule that cannot fail a
check is not a rule, so mine.mjs must REFUSE it: this doc yields zero candidates
(witness-or-drop + unfalsifiable-refusal). The real, code-backed invariant in this
fixture lives in `src/` and is what the miner surfaces instead.
